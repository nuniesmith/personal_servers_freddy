# ================================================
# ENHANCED ERROR LOCATIONS CONFIGURATION - FIXED
# ================================================
# Modern, user-friendly error pages with consistent styling
# 
# IMPORTANT: These location blocks must be added directly to server blocks
# They CANNOT be included via include files due to nginx limitations
# Copy these location blocks directly into each server block that needs them

# ================================================
# CUSTOM ERROR PAGES (INTERNAL ACCESS ONLY)
# ================================================

# 404 - Not Found
location = /errors/404.html {
    root /usr/share/nginx/html;
    internal;
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 404 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: bounce 2s infinite; }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
            border: 2px solid transparent;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            border-color: rgba(255, 255, 255, 0.3);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">🔍</div>
        <h1>404</h1>
        <div class="subtitle">Page Not Found</div>
        <p>The page you are looking for does not exist or has been moved.</p>
        <p>This might be a typo in the URL, or the service might be temporarily unavailable.</p>
        <div class="buttons">
            <a href="https://7gram.xyz" class="primary">🏠 Dashboard</a>
            <a href="javascript:history.back()">← Go Back</a>
            <a href="https://7gram.xyz/api/discover-services">🔍 Browse Services</a>
        </div>
        <div class="error-code">Error 404 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# 500 - Internal Server Error
location = /errors/500.html {
    root /usr/share/nginx/html;
    internal;
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 500 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Server Error | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: shake 1s infinite; }
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">⚠️</div>
        <h1>500</h1>
        <div class="subtitle">Internal Server Error</div>
        <p>Something went wrong on our end. The service is temporarily unavailable.</p>
        <p>Our team has been notified and is working to fix this issue.</p>
        <div class="buttons">
            <a href="https://7gram.xyz" class="primary">🏠 Dashboard</a>
            <a href="javascript:location.reload()">🔄 Retry</a>
            <a href="https://7gram.xyz/status">📊 Service Status</a>
        </div>
        <div class="error-code">Error 500 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# 502 - Bad Gateway
location = /errors/502.html {
    root /usr/share/nginx/html;
    internal;
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 502 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>502 - Service Unavailable | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #ffa726 0%, #ff7043 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: pulse 2s infinite; }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">🔌</div>
        <h1>502</h1>
        <div class="subtitle">Service Unavailable</div>
        <p>The service is temporarily offline or restarting.</p>
        <p>This usually resolves itself within a few minutes. Please try again shortly.</p>
        <div class="buttons">
            <a href="https://7gram.xyz" class="primary">🏠 Dashboard</a>
            <a href="javascript:location.reload()">🔄 Retry</a>
            <a href="https://7gram.xyz/api/service-status">📊 Check Status</a>
        </div>
        <div class="error-code">Error 502 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# 503 - Service Temporarily Unavailable
location = /errors/503.html {
    root /usr/share/nginx/html;
    internal;
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 503 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>503 - Maintenance Mode | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #42a5f5 0%, #478ed1 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: spin 3s linear infinite; }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">🔧</div>
        <h1>503</h1>
        <div class="subtitle">Maintenance Mode</div>
        <p>This service is temporarily down for maintenance.</p>
        <p>We are making improvements and will be back shortly!</p>
        <div class="buttons">
            <a href="https://7gram.xyz" class="primary">🏠 Dashboard</a>
            <a href="javascript:location.reload()">🔄 Check Again</a>
            <a href="https://7gram.xyz/api/service-status">📊 Service Status</a>
        </div>
        <div class="error-code">Error 503 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# 504 - Gateway Timeout
location = /errors/504.html {
    root /usr/share/nginx/html;
    internal;
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 504 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>504 - Request Timeout | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #ab47bc 0%, #8e24aa 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: tick 1s infinite; }
        @keyframes tick {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0.3; }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">⏱️</div>
        <h1>504</h1>
        <div class="subtitle">Request Timeout</div>
        <p>The service took too long to respond.</p>
        <p>This might be due to high server load or network issues. Please try again.</p>
        <div class="buttons">
            <a href="https://7gram.xyz" class="primary">🏠 Dashboard</a>
            <a href="javascript:location.reload()">🔄 Try Again</a>
            <a href="https://7gram.xyz/api/service-status">📊 Service Status</a>
        </div>
        <div class="error-code">Error 504 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# ================================================
# AUTHENTICATION ERROR HANDLERS
# ================================================

# 401 - Authentication Required
location @error401 {
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 302 https://auth.7gram.xyz/?rd=$scheme://$http_host$request_uri;
}

# 403 - Access Forbidden (Enhanced)
location @error403 {
    add_header Content-Type "text/html; charset=utf-8" always;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    
    return 403 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>403 - Access Forbidden | 7Gram Network</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .icon { font-size: 4rem; margin-bottom: 1rem; animation: sway 2s ease-in-out infinite; }
        @keyframes sway {
            0%, 100% { transform: rotate(-3deg); }
            50% { transform: rotate(3deg); }
        }
        h1 { font-size: 4rem; margin-bottom: 1rem; font-weight: 700; }
        .subtitle { font-size: 1.5rem; margin-bottom: 1rem; opacity: 0.9; }
        p { font-size: 1.1rem; margin-bottom: 1.5rem; opacity: 0.8; line-height: 1.6; }
        .auth-info {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 1rem;
            margin: 1.5rem 0;
            font-size: 0.95rem;
        }
        .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        a { 
            color: white; 
            text-decoration: none; 
            background: rgba(255, 255, 255, 0.2);
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            font-weight: 500;
            border: 2px solid transparent;
        }
        a:hover { 
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            border-color: rgba(255, 255, 255, 0.3);
        }
        .primary { background: rgba(255, 255, 255, 0.3) !important; }
        .error-code { font-size: 0.9rem; opacity: 0.7; margin-top: 1rem; }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">🔒</div>
        <h1>403</h1>
        <div class="subtitle">Access Forbidden</div>
        <p>You do not have permission to access this resource.</p>
        <div class="auth-info">
            <strong>Possible reasons:</strong><br>
            • You need different authentication credentials<br>
            • This service requires two-factor authentication<br>
            • Your account may not have the required permissions<br>
            • The service may be restricted to admin users only
        </div>
        <div class="buttons">
            <a href="https://auth.7gram.xyz/?rd=$scheme://$http_host$request_uri" class="primary">🔑 Login Again</a>
            <a href="https://7gram.xyz">🏠 Dashboard</a>
            <a href="javascript:history.back()">← Go Back</a>
        </div>
        <div class="error-code">Error 403 • 7Gram Network</div>
    </div>
</body>
</html>';
}

# ================================================
# IMPORTANT USAGE NOTES
# ================================================

# THESE LOCATION BLOCKS MUST BE COPIED DIRECTLY INTO YOUR SERVER BLOCKS
# 
# You CANNOT use: include /etc/nginx/includes/error_locations.conf;
# 
# Instead, copy the location blocks above directly into each server block:
#
# server {
#     listen 443 ssl;
#     server_name example.7gram.xyz;
#     
#     # Include error page directives only
#     include /etc/nginx/includes/error_pages.conf;
#     
#     # Copy all the location blocks from above here
#     location = /errors/404.html { ... }
#     location = /errors/500.html { ... }
#     # ... etc
#     
#     # Your other location blocks
#     location / { ... }
# }
#
# Fixed issues:
# ✅ Removed all contractions with single quotes
# ✅ All HTML content now uses proper escaping
# ✅ Added clear usage instructions
# ✅ Maintained the same beautiful styling and functionality